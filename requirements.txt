# One pure-Python dependency, pinned with a hash so Hermeto's pip prefetch works.
# Its sdist lands in output/deps/pip/ as an archive (six-1.16.0.tar.gz) and has
# ALWAYS been included in the source container -- it is the "control" that proves
# the source-build task is running, so any missing cargo/ sources stand out.
six==1.16.0 \
    --hash=sha256:1e61c37477a1626458e36f7b1d82aa5c9b094fa4802892072e49de9c60c4c926
